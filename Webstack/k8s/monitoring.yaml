apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-monitor
  namespace: project-webstack
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: api-service 
  endpoints:
  - port: http 
    path: /metrics